# Frontend environment — copy to .env
# Restart `npm run dev` after changing VITE_* variables.
#
# Local:     http://localhost:8000/api
# Production (Vercel): set this in the Vercel project env to your Render API, e.g.
#            https://manaksetu-api.onrender.com/api

VITE_API_BASE_URL=http://localhost:8000/api
